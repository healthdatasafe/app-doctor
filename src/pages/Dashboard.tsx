import {
  Box,
} from "@mui/material";

export function Dashboard() {

  return (
    <Box
      sx={{
        p: 3,
        height: "100%",
        display: "flex",
        flexDirection: "column",
      }}
    >
        Hello
    </Box>
  );
}
